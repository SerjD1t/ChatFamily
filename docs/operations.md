# Разработка и безопасное развёртывание

Правила работы: [AGENTS.md](../AGENTS.md). Последние выполненные действия и проверки — в [work-log.md](../work-log.md); запись в журнале не заменяет проверку текущего сервера.

## Конфигурация

Единственный разрешённый локальный файл с секретами — корневой settings.local.json, исключённый из Git и Docker-контекста. Значения не выводить и не копировать в другие файлы проекта. Имена переменных окружения перечислены в README; .env.example — только справочный шаблон.

Основная Go-конфигурация читается из окружения (internal/config/config.go). Загрузки всех параметров из settings.local.json пока нет; отдельный загрузчик используется для Firebase. Для локального запуска заранее настройте изолированную PostgreSQL и безопасно передайте переменные процессу, без значений в командах/истории терминала, затем выполните:

```sh
go run .
```

Важно: текущий docker-compose.yml содержит обязательный env_file: .env. Поэтому запуск Compose на чистом рабочем каталоге не является готовым способом запуска при новом правиле локальных секретов. Не создавайте .env с копией settings.local.json: адаптацию конфигурации Compose нужно выполнить отдельной задачей. Действующее серверное окружение не удалять и не переписывать автоматически.

Проверка исключения без чтения секретов:

```sh
git check-ignore settings.local.json
```

## Проверки

Ежедневные отчёты пользователей: [личная автоматизация](automations.md), миграция 027.
Планировщик работает в app, отдельный cron не требуется; резервное копирование
в 02:30 независимо от пользовательских расписаний. Настройки по умолчанию выключены.

Android APK публикуются отдельно от данных чата: [обновления клиента](mobile/updates.md).
В Compose добавлен read-only каталог downloads/android; первый деплой механизма требует
нового серверного бинарника и пересоздания app через штатную обёртку. APK и latest.json
готовятся локально, на сервер переносится сначала APK, описание — последним.

Автономное резервное копирование с Яндекс Диском: [настройка, ограничения и восстановление](backups.md).
Включено на сервере 2026-09-17: ежедневно 02:30 Europe/Moscow. BACKUP_CONTROL_DIR подключает
только несекретный каталог управления к web-приложению; секреты остаются у host worker.

```sh
go test ./...
go build ./...
node --check web/app.js
node --test web/*.test.mjs
git diff --check
```

DOM-тестам нужен jsdom: установить в отдельный тестовый каталог и передать путь к модулю через TEST_JSDOM_PATH. Проверять число пропусков.
Интеграционным тестам нужен TEST_DATABASE_URL, безопасно переданный через окружение, только на отдельную тестовую БД. Они применяют миграции и создают/удаляют фикстуры; не запускать на рабочей базе.

## Чек-лист развёртывания

1. Получить запрос на обновление сервера. Подтвердить целевой хост по локальным настройкам, домен и текущую версию. Не печатать параметры доступа.
2. Запустить тесты. Собрать Go локально/CI для Linux amd64 с CGO_ENABLED=0; не компилировать на VPS с рабочими сервисами.
3. Создать резервную копию PostgreSQL, проверить её читаемость; учесть сохранность тома вложений. Сохранить предыдущий образ и конфигурацию запуска.
4. Перенести бинарник, web/ и deploy/Dockerfile.runtime в отдельный каталог. Собрать runtime-образ с уникальным тегом и явно выбрать его в конфигурации запуска.
5. Обновить только app командой `python3 deploy/compose.py up -d --no-build --no-deps app` из /opt/chatfamily. Обёртка передаёт только Firebase serviceAccount из settings.local.json через окружение и сохраняет backup override. Не возвращать bind mount settings.local.json в app: в нём теперь host-only ключи бэкапа. Не удалять тома и не пересоздавать сеть без отдельной необходимости. Миграции встроены в бинарник и применяются при старте.
6. Проверить health, HTTPS, нужные статические ресурсы, версию образа, контейнеры, миграции, ошибки и память. Для изменённого функционала выполнить безопасную приёмку.
7. Записать фактический результат и версию в журнал. Откат образа не равнозначен откату схемы: совместимость с новой миграцией проверять отдельно.

SSH — по ключу с проверкой хоста; парольный вход остаётся отключённым. TLS не отключать. В production Compose зафиксирован MTU 1450; на другом хостинге проверять фактический MTU, не считать число универсальным.
Git commit/push, релиз и развёртывание — независимые операции.

Миниатюры вложений после v0.10.0 опубликованы 2026-09-16: runtime chatfamily-app:20260916-previews с FFmpeg, Poppler и prlimit. Конвертеры проверены под лимитами Linux до переключения приложения. Для сборки использован docker build --network host: проверка показала доступ к Alpine-репозиторию из host и таймаут из стандартной bridge-сети. Рабочая сеть/MTU не менялись. При последующих сборках повторно проверить доступ и свободное место; см. docs/attachment-previews.md.

## Версии и ограничения

Код релиза: **v0.12.0**, см. [описание](releases/v0.12.0.md). Публикация в GitHub не выполняет развёртывание.

Последнее развёртывание: **2026-09-18**, `chatfamily-app:20260918-messaging`:
базовые сообщения/вложения без флагов, защита приглашений и изменения сообщений,
сортировка личных диалогов, исключение автора реакции из Web/Android push.
Обновлены локально собранный Linux amd64 бинарник, app.js и directory.js.
Схема 027, APK и расписание бэкапов 02:30 не менялись. Проверены Go, 58 web-тестов,
HTTPS/health, точные ресурсы/хеш бинарника, контейнеры и логи. Резервные копии
before-20260918-messaging; образ отката rollback-before-messaging.

Предыдущее развёртывание: **2026-09-18**, `chatfamily-app:20260918-quick-needs`:
быстрый срок и исполнитель в карточке. Заменены app.js, family-needs.js и style.css;
бинарник, схема 027 и APK не менялись. 57 web-тестов без пропусков; проверены HTTPS,
точные ресурсы, контейнеры, логи, бэкапы и изоляция секретов. Архивы
before-20260918-quick-needs, откат rollback-before-quick-needs. Без push/релиза.

Предыдущее развёртывание: **2026-09-18**, `chatfamily-app:20260918-need-transfer`:
смена личной/семейной области существующей записи автором с подтверждением.
Перенесены новый Linux amd64 бинарник, app.js и family-needs.js, схема остаётся 027.
Проверены Go и 54 web-теста; PostgreSQL-проверки переноса прошли на отдельной БД
при реализации. После обновления проверены HTTPS/health, хеш бинарника и ресурсы,
контейнеры/логи, сохранность бэкапов 02:30 и изоляция секретов.
Архивы before-20260918-need-transfer; образ отката rollback-before-need-transfer.
APK не менялся, Git push/релиз не выполнялись.

Предыдущее развёртывание: **2026-09-18**, `chatfamily-app:20260918-unified-needs`:
единый список личных и семейных дел, пиктограммы области, фильтр внутри поиска,
явный выбор области при создании (по умолчанию личное). Заменены только app.js
и family-needs.js; бинарник, схема 027, APK и расписание бэкапов не менялись.
Проверены 52 web-теста без пропусков, HTTPS и точные ресурсы, контейнеры/логи.
Архивы before-20260918-unified-needs; откат rollback-before-unified-needs.
Изменение после v0.12.0, без нового Git push/релиза.

Предыдущее развёртывание: **2026-09-18**, `chatfamily-app:20260918-v012`, схема **027**.
Обновлены бинарник и web/: личные дела, автоматизация отчёта, переключение семей,
исправления ширины/полей iPhone и поддержка обновления Android. До переключения
проверены все store-тесты на отдельной PostgreSQL без сети и 51 web-тест.
Архивы: `/opt/chatfamily-backups/before-20260918-v012` (.dump, -uploads.tar,
-launch.tar); образ отката `chatfamily-app:rollback-before-v012`. При откате
сохранять схему 026–027 и новые данные; не восстанавливать старую БД поверх сообщений.
Проверены HTTPS/health, точное содержимое ресурсов, миграции, отсутствие ошибок
и перезапусков, изоляция секретов, неизменное расписание бэкапов; доступно около
598 МБ RAM и 2,3 ГБ диска. Это результат проверки при деплое, не постоянный мониторинг.

Android **0.5.0 / versionCode 5** опубликован отдельно в read-only каталоге
`/opt/chatfamily/downloads/android`: [скачать APK](https://www.chatfamily.site/downloads/android/chatfamily-5.apk).
APK проверен по SHA-256 после скачивания по HTTPS, `latest.json` опубликован последним
с `Cache-Control: no-store`. Это debug-сборка; первая установка механизма вручную.
Физическая приёмка установки поверх старого APK и Safari ещё требуется.

Предыдущее развёртывание: **2026-09-17**, runtime `chatfamily-app:20260917-no-family`, схема **025**. Исправлены личные диалоги без семьи; также включены предыдущие доработки оформления, семей и прочтений. Резервные копии `before-20260917-no-family` (.dump, -uploads.tar, -launch.tar) находятся в /opt/chatfamily-backups; образ отката `chatfamily-app:rollback-before-no-family`. Тогда проверены Go/41 web-тест, HTTPS/health, точные ресурсы, образ и отсутствие ошибок/перезапусков. Это запись о выполненной проверке, а не мониторинг текущего состояния.

Обновление после записи выше: **2026-09-17**, runtime `chatfamily-app:20260917-backups`, схема 025 без новых миграций. Бэкапы включены ежедневно в 02:30 Europe/Moscow; первая копия и изолированное восстановление PostgreSQL/вложений проверены. Служба host worker/systemd, restic 0.19.1, ограниченная папка Яндекс Диска. Предварительные локальные архивы: `before-20260917-backups`; образ отката `chatfamily-app:rollback-before-backups`. При откате не возвращать старый bind mount настроек в app. Секреты не включать в образ, Git и release assets. См. [backup runbook](backups.md).

### История последних развёртываний

Предыдущее обновление: `chatfamily-app:20260917-users-panel` (2026-09-17): отдельный
экран пользователей в администрировании. Перенесены web/app.js и web/index.html,
сохранены мобильная навигация, backup override и изоляция ключей. Архивы перед
обновлением: `before-20260917-users-panel`, откат: `rollback-before-users-panel`.
Проверены 45 web-тестов, HTTPS, точные ресурсы, контейнеры и бэкапы 02:30; схема 025.

Предыдущее обновление: `chatfamily-app:20260917-mobile-reopen` (2026-09-17), исправление
повторного входа в тот же чат/дела на мобильном экране. Заменён только web/app.js
слоем поверх предыдущего runtime; бинарник и схема 025 не менялись. Архивы перед
обновлением: `before-20260917-mobile-reopen`, образ отката `rollback-before-mobile-reopen`.
Проверены HTTPS, точное содержимое app.js, контейнеры/логи и сохранность бэкапов 02:30.

| Дата | Runtime-суффикс | Изменение |
| --- | --- | --- |
| 2026-09-17 | no-family | Видимость личных диалогов без семьи |
| 2026-09-17 | global-families | Управление всеми семьями, без автоматического доступа к переписке |
| 2026-09-17 | member-editor | Отдельное редактирование участника семьи |
| 2026-09-17 | appearance-range / appearance | Скины, плотность, размер текста |
| 2026-09-17 | family-labels | Миграция 025, подпись «Статус (Имя)» |
| 2026-09-17 | receipt-details | Подробности доставки и прочтения |
| 2026-09-17 | needs-list | Список дел первым экраном |
| 2026-09-16 | previews / storage | Миниатюры и обслуживание хранилища |

Подробные результаты, резервные копии и образы отката каждого шага сохранены в work-log.md. Исторические runtime-метки в тематической справке означают первое развёртывание функции, не текущий образ.

Миграция 025 изменяет SQL-функцию семейной подписи. Откат одного образа не возвращает старый формат: при необходимости отдельно восстановить прежнее определение функции, не восстанавливая старую БД поверх новых сообщений.

Парольная и keyboard-interactive аутентификация SSH отключены 2026-09-16. Изменены ранний 00-chatfamily-key-only.conf и Match-блок 99-qemu.conf; синтаксис, эффективные настройки и новый вход по ключу тогда проверены. После изменений cloud-init/провайдера повторно проверять эффективную конфигурацию.

Для удалённой Android-оболочки 0.3.0+ правки web/ не требуют нового APK; нативные возможности зависят от capabilities установленной версии. Визуальная приёмка на физических Android/Safari остаётся отдельной проверкой.
